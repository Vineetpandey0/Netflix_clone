module.exports = {
    plugins: [
      require('tailwindcss'), // This tells PostCSS to use Tailwind
      require('autoprefixer'), // This adds vendor prefixes for better browser support
    ],
  }
  